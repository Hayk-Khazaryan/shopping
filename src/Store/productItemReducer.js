import data from "./data"

const intialState = {
       productItem: data
}

export function productItemReducer(state = intialState, action) {
       switch (action.type) {
              default:
                     return state
       }
}